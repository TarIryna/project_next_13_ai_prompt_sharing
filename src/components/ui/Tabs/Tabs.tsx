import type { ITabsProps } from "./types";
import type { FC } from "react";
import * as S from "./styles";

const Tabs: FC<ITabsProps> = ({ tabs, activeTab }) => {
  return <S.TabsWrapper></S.TabsWrapper>;
};

export default Tabs;
